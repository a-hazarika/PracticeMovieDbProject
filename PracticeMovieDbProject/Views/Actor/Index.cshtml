@model ActorsListingViewModel
@{
    ViewBag.Title = "Actors";
}

<div class="row">
    <div class="col-md-12">
        <h3 class="text-muted">@ViewBag.Title</h3>
        <div id="moviesTable">
            @foreach (var actor in Model.Actors)
            {
                <hr />
                <div class="row padded">
                    <div class="col-md-12">
                        <div class="col-sm-2">
                            <img class="squareImage" src="~/images/posters/No_Image_Available.gif" />
                        </div>
                        <div class="col-sm-10">
                            <a asp-controller="Actor" asp-action="Detail" asp-route-id="@actor.Id"><strong>@actor.Name</strong></a> (@actor.Sex, @actor.Age years)
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

